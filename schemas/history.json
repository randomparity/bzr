{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/randomparity/bzr/schemas/history.json",
  "title": "History Change Record",
  "description": "One flattened bug-history change record, as emitted in `bzr bug history --json` / `--output ndjson`. A history entry with N changed fields expands to N records sharing `when`/`who`/`comment_id`.",
  "type": "object",
  "properties": {
    "when": {
      "type": "string",
      "description": "ISO 8601 timestamp of the change, verbatim from the server."
    },
    "who": {
      "type": "string",
      "description": "The user who made the change."
    },
    "field": {
      "type": "string",
      "description": "The changed field name (e.g. `status`, `assigned_to`, or a `cf_*` custom field)."
    },
    "old_value": {
      "type": ["string", "null"],
      "description": "The removed value; empty string when nothing was removed, null when the server omitted the value."
    },
    "new_value": {
      "type": ["string", "null"],
      "description": "The added value; empty string when nothing was added, null when the server omitted the value."
    },
    "comment_id": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "The id of a comment posted in the same history entry (correlated by author and timestamp), or null when no comment correlates."
    }
  },
  "required": [
    "when",
    "who",
    "field",
    "old_value",
    "new_value",
    "comment_id"
  ],
  "additionalProperties": false
}
