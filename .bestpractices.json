{
  "static_analysis_status": "Met",
  "static_analysis_justification": "The project applies multiple FLOSS static analysis tools as a merge gate on every proposed change, and releases are cut only from green CI:\n - Clippy (FLOSS) runs via the clippy job in .github/workflows/ci.yml as cargo clippy --locked --all-targets --features test-helpers -- -D warnings, with strict pedantic lint rules enabled project-wide ([lints.clippy] in Cargo.toml; unwrap_used denied). Warnings fail the build.\n - SonarCloud (SonarQube, listed by this criterion as an example tool) scans the repo on pushes to main and non-fork PRs via the sonarqube job (SonarSource/sonarqube-scan-action), configured in sonar-project.properties.\n - Shell scripts (install.sh) are analyzed with shellcheck and PowerShell scripts with PSScriptAnalyzer in the same workflow.\nSince CI must be green before a release branch/tag is cut, these tools are applied to every major release.",
  "static_analysis_common_vulnerabilities_status": "Met",
  "static_analysis_common_vulnerabilities_justification": "Two of the applied tools look for common vulnerabilities:\n - SonarCloud/SonarQube applies security-focused rulesets including taint analysis to the scanned code.\n - cargo-deny (.github/workflows/dependency-policy.yml, cargo deny check on every push and PR) checks all dependencies against the RustSec Advisory Database, detecting known vulnerabilities in the crates the software is built on.",
  "static_analysis_fixed_status": "Met",
  "static_analysis_fixed_justification": "Project policy: confirmed medium-or-higher severity vulnerabilities found by static analysis or dependency auditing (cargo-deny/RustSec, SonarCloud, Dependabot) are fixed in a timely way, well within the 60-day window required by vulnerabilities_60_days. Enforcement is structural: Clippy runs with -D warnings and cargo-deny fails CI on advisories at the configured severity, so no release can be cut while a flagged issue is unresolved — fixing it is the only path to a green build. No confirmed medium+ static-analysis vulnerabilities are currently outstanding.",
  "static_analysis_often_status": "Met",
  "static_analysis_often_justification": "Static analysis runs on essentially every commit: both workflows trigger on: push to main and on: pull_request to main. The Clippy job runs unconditionally on every event (including forked and Dependabot PRs — noted explicitly in the workflow as the SAST backstop), and SonarCloud scans every push to main."
}
